# App Service Rollout Plan

Plan for getting Django running on Azure App Service with the Postgres backend.
This doc exists so we can resume mid-task across sessions.

## The 4 phases

### Phase 1 — Web App Terraform module
Create `modules/web-app/` that:
- Creates `azurerm_linux_web_app` running the container image from ACR
- Sets up VNet integration so the app can reach private Postgres
- Uses system-assigned managed identity
- Generates Django `SECRET_KEY`, stores in Key Vault
- Sets app settings (env vars) the app needs at boot

### Phase 2 — Wire dependencies in dev environment
- ACR pull: grant the web app's managed identity `AcrPull` role
- Key Vault: grant the managed identity `Get/List` on secrets
- Postgres: pass FQDN + credentials so the app builds `DATABASE_URL`
- Network: pass `app_subnet_id` for VNet integration

### Phase 3 — Image version control
- `image_version` variable per environment
- Manual promotion model: change the version in tfvars / variables.tf default → PR → merge → deploy
- Default to `latest` for dev only; pin specific versions for qa/prod when enabled

### Phase 4 — Deployment slot (blue-green swap)
- Add a `staging` slot alongside production
- Deploys go to staging first, then swap
- Verify health before swap, instant rollback by swap-back
- Out of scope for branch 1 — separate PR

## Current status (as of branch `feat/web-app-module`)

### Done — Phases 1, 2, 3 (in this branch, not yet merged)

**New files:**
- `modules/web-app/versions.tf`
- `modules/web-app/variables.tf`
- `modules/web-app/main.tf` — web app + Key Vault access policy + AcrPull role assignment + SECRET_KEY generation
- `modules/web-app/outputs.tf` — `name`, `default_hostname`, `url`, `principal_id`

**Modified files:**
- `modules/postgres-flexible/outputs.tf` — added sensitive `administrator_password` output
- `environments/dev/main.tf` — wired `web_app` module + ACR data source
- `environments/dev/variables.tf` — added `image_version`, `acr_name`, `acr_resource_group_name`

**Resource names:**
- Web app: `app-iw-wings-dev` → reachable at `https://app-iw-wings-dev.azurewebsites.net`

**App settings injected:**
- `WINGS_SETTINGS=azure` — tells Django to load `settings_azure.py`
- `WINGS_ENV=dev` — shown on the home page badge
- `WEBSITES_PORT=8000` — gunicorn port
- `SECRET_KEY` — generated by `random_password`, also stored in Key Vault
- `DATABASE_URL` — built from postgres FQDN + admin login + password
- `ALLOWED_HOSTS` — set to the App Service default hostname
- `WEBSITES_ENABLE_APP_SERVICE_STORAGE=false` — disable App Service file system mount (unused)

### Done in `wings/` repo (already merged)

- `wings/settings.py` — overlay selector at the end (reads `WINGS_SETTINGS`)
- `wings/settings_azure.py` — DEBUG=False, reads SECRET_KEY, ALLOWED_HOSTS, DATABASE_URL from env vars
- `pyproject.toml` — added `psycopg2-binary`, `dj-database-url`
- `templates/home.html` + `views.py` — env + version badges on home page
- ACR rename: `acrwings01` → `acriwwings01`
- Build pipeline pushes to new ACR

### Pending — Phase 4 (next session)

**Goal:** Full CI/CD blue-green. Every deploy goes to staging slot first, gets health-checked, then swaps to production. Zero manual `az` commands.

**The flow:**
```
Code change in wings/ → image built and pushed to ACR (v0.5.0)
              ↓
PR in wings_deployment/ bumps image_version, merged
              ↓
Deploy pipeline runs:
  1. terraform apply — infra changes (NOT image)
  2. az webapp config container set --slot staging --image wings:0.5.0
  3. Wait + health check on https://app-iw-wings-dev-staging.azurewebsites.net
  4. az webapp deployment slot swap
              ↓
Production now runs v0.5.0. Old v0.4.0 sits in staging (instant rollback ready).
```

**Terraform changes (`modules/web-app/main.tf`):**
- Add `azurerm_linux_web_app_slot` named `staging`
- Slot needs:
  - Its own `identity { type = "SystemAssigned" }` block
  - Same VNet integration via `virtual_network_subnet_id`
  - Same `app_settings` map as the main web app
  - Same `container_registry_use_managed_identity = true`
- **Critical:** Add `lifecycle { ignore_changes = [site_config[0].application_stack] }` on **both** the main web app AND the staging slot — so Terraform doesn't fight with CI/CD over image versions
- Add `azurerm_key_vault_access_policy` granting the slot's identity `Get`/`List` on Key Vault
- Add `azurerm_role_assignment` granting the slot's identity `AcrPull` on ACR

**Pipeline changes (`.github/workflows/_apply-env.yml`):**

After the existing `Terraform Apply` step, add three new steps (all guarded by `if: steps.check.outputs.enabled == 'true'`):

1. **Set staging slot image:**
   ```yaml
   - name: Set staging slot image
     run: |
       az webapp config container set \
         --name app-iw-${{ inputs.environment == 'sb' && 'wings-sb' || format('wings-{0}', inputs.environment) }} \
         --resource-group rg-iw-wings-${{ inputs.environment }} \
         --slot staging \
         --container-image-name acriwwings01.azurecr.io/wings:${{ env.IMAGE_VERSION }}
   ```

2. **Health check staging:**
   ```yaml
   - name: Health check staging slot
     run: |
       URL=https://app-iw-wings-${{ inputs.environment }}-staging.azurewebsites.net
       for i in $(seq 1 30); do
         if curl -sf -o /dev/null -w "%{http_code}" $URL | grep -q "200"; then
           echo "Staging healthy"; exit 0
         fi
         echo "Attempt $i: not ready, retry in 10s"; sleep 10
       done
       echo "Staging never became healthy"; exit 1
   ```

3. **Swap slots:**
   ```yaml
   - name: Swap staging to production
     run: |
       az webapp deployment slot swap \
         --name app-iw-wings-${{ inputs.environment }} \
         --resource-group rg-iw-wings-${{ inputs.environment }} \
         --slot staging \
         --target-slot production
   ```

**First-deploy bootstrap:**
The very first deploy ever — production slot has no image yet. Options:
- (a) **Preferred:** Pipeline detects "no image set on production yet" and does direct deploy to production for first deploy. Use `az webapp config container show` to check.
- (b) **Simpler:** Manually set the production image once via az CLI, then the staging-then-swap flow handles everything from then on.

**One subtle thing:**
- App settings that point to env-specific resources (DATABASE_URL, Key Vault refs) should be marked as **slot settings** (sticky to the slot) so they don't swap when slots swap. Both slots talk to the same DB — they shouldn't trade DB URLs.
- In Terraform: `sticky_settings { app_setting_names = ["DATABASE_URL", "ALLOWED_HOSTS"] }` on the main web app.

**Test plan:**
- [ ] Terraform creates both slots; both run the current production image initially
- [ ] Manual test: push new image version, watch pipeline deploy to staging, swap, production now runs new version
- [ ] Manual rollback test: `az webapp deployment slot swap` again → production reverts to previous version

**Estimated changes:**
- 1 new resource block in `modules/web-app/main.tf` (~30 lines for the slot)
- 2 new resource blocks for slot's KV access + AcrPull
- `lifecycle { ignore_changes }` block on both web app and slot
- 1 `sticky_settings` block
- 3 new steps in `_apply-env.yml`
- Total: ~80 lines across 2 files

## What still might break — open items

1. **Container start health check** — App Service may probe `/` before Django is ready. May need to add a `WEBSITES_HEALTHCHECK_MAXPINGFAILURES` or similar.
2. **First-boot migrations** — `python manage.py migrate` is not run anywhere yet. The DB exists but has no Django tables. Need a startup hook or manual run.
3. **Static files** — `collectstatic` runs at Docker build time but Whitenoise (or similar) is needed to serve them. Currently `STATIC_ROOT` is set but no serving middleware.
4. **HTTPS-only / TLS settings** — App Service serves HTTPS by default but Django settings should set `SECURE_PROXY_SSL_HEADER`.

## How to test locally before merging

```bash
cd /home/ashish/project/wings_deployment/environments/dev
source /home/ashish/project/auth-azure.sh
terraform init -backend-config=../../bootstrap/backend.hcl
terraform plan
```

A successful plan should show the new `module.web_app.*` resources and the ACR data source lookup.

## How to verify after deploy

```bash
# Image is pulled and container starts
curl https://app-iw-wings-dev.azurewebsites.net/

# Should show the home page with badges:  env: dev    v0.4.0
```

If the page shows but DB queries fail, the issue is likely migrations (see open item #2).
